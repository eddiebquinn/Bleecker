- name: Update and upgrade APT packages
  hosts: all
  become: true
  tasks:
    - name: Update and upgrade APT packages
      become: true
      ansible.builtin.apt:
        update_cache: yes
        upgrade: dist
