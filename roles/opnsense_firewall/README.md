# opnsense_firewall

OPNsense firewall role for blackice-1. Manages firewall rules, aliases,
interface configurations, and NAT rules via the `opnsense.ansible` collection.

Requires the OPNsense API to be enabled and an API key configured.
