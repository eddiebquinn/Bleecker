---
# Handlers for routeros_l3_switch
#
# RouterOS applies changes immediately — there is no commit/confirm model
# like some other network OSes. These handlers exist for future use with
# RouterOS scripts or notification patterns.
#
# Currently a placeholder — no handlers are needed for direct CLI commands.
